{
  "name": "picker",
  "version": "1.0.0",
  "description": "Choose random slack users with ease!",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "node app.ts",
    "start": "pm2 start app.js --name='slack-bot'",
    "delete": "pm2 delete slack-bot"
  },
  "author": "aramshiva",
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.18.0",
    "dotenv": "^16.4.5"
  }
}
